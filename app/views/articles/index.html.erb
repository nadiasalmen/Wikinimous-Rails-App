<div class="container col-5 py-3">
  <% @articles.each do |article| %>
    <h2><%= link_to article.title, article_path(article), :class => "text-info"  %></h2>
    <p><%= article.content %></p>
  <% end %>
</div>
